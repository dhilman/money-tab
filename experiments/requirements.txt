# Core dependencies
pydantic>=2.0
litellm>=1.0
pillow>=10.0      # For image compression

# OCR options (optional, for run_ocr_then_llm.py)
pytesseract>=0.3  # Requires: brew install tesseract
easyocr>=1.7      # Slower to install, larger models

# Optional: for running all experiments
# torch>=2.0       # Required by easyocr
